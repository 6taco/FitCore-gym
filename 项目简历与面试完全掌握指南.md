# 健身房管理系统：简历与面试完全掌握指南

## 一、项目一句话概括

这是一个基于 **React + Node.js/Express + MySQL** 的前后端分离健身房管理系统，覆盖会员管理、会籍卡种、课程预约、教练排课、入场签到、商品库存、收银订单、财务报表、数据看板、通知中心和系统权限管理等业务场景。

面试时可以这样说：

> 我做的是一个健身房综合管理系统，采用前后端分离架构。前端使用 React 18、Vite、TypeScript、Ant Design 和 Zustand，后端使用 Node.js、Express、Sequelize 和 MySQL。系统实现了 JWT 登录认证、RBAC 角色权限、会员会籍、课程预约、商品库存、收银订单、财务报表、数据看板、Excel 导入导出和 Docker Compose 部署等功能。

---

## 二、项目整体架构

### 1. 总体分层

项目可以分为三层：

- **前端层**：`client/`
- **后端层**：`server/`
- **数据库层**：`db/`

整体链路是：

```text
用户操作页面
  ↓
React 前端收集数据
  ↓
Axios 调用 /api 接口
  ↓
Express 后端路由接收请求
  ↓
JWT 登录校验 + RBAC 权限校验
  ↓
Controller 处理业务逻辑
  ↓
Sequelize 操作 MySQL
  ↓
数据库返回结果
  ↓
后端统一封装 JSON
  ↓
前端渲染页面
```

通俗理解：

- **前端**：负责用户看得见、点得到的界面。
- **后端**：负责判断能不能做、怎么做、做完怎么存。
- **数据库**：负责长期保存会员、课程、订单、权限等核心数据。

---

## 三、项目目录结构应该怎么熟悉

### 1. 根目录

```text
健身房管理系统/
├─ client/                 前端 React 项目
├─ server/                 后端 Express 项目
├─ db/                     数据库建表脚本和迁移脚本
├─ docs/                   项目文档
├─ docker-compose.yml      Docker Compose 一键部署配置
├─ package.json            根脚本，用于同时启动前后端
├─ README.md               项目说明
├─ 项目说明文档.md
├─ 项目整体架构面试回答.txt
└─ 项目简历与面试完全掌握指南.md
```

### 2. 前端目录

```text
client/src/
├─ api/                    接口请求封装
├─ components/             通用组件
├─ config/                 菜单、权限、配置
├─ layouts/                主布局
├─ pages/                  页面模块
├─ router/                 前端路由和路由守卫
├─ stores/                 Zustand 状态管理
├─ styles/                 全局样式
├─ App.tsx                 应用入口组件
└─ main.tsx                React 挂载入口
```

前端重点文件：

- `client/src/api/request.ts`：Axios 实例、请求拦截器、响应拦截器、token 自动携带、refreshToken 刷新。
- `client/src/stores/authStore.ts`：登录状态、用户信息、权限判断。
- `client/src/router/index.tsx`：前端路由配置。
- `client/src/router/guards.tsx`：登录守卫和权限守卫。
- `client/src/layouts/MainLayout.tsx`：后台主布局、菜单、顶部栏、通知铃铛、用户下拉菜单。
- `client/src/config/menu.tsx`：侧边栏菜单和权限码配置。

### 3. 后端目录

```text
server/src/
├─ config/                 环境变量和数据库连接
├─ constants/              常量
├─ controllers/            控制器，处理业务逻辑
├─ jobs/                   定时任务
├─ middleware/             中间件
├─ models/                 Sequelize 模型
├─ routes/                 路由模块
├─ utils/                  工具函数
├─ app.js                  Express 应用配置
└─ index.js                服务启动入口
```

后端重点文件：

- `server/src/index.js`：启动服务、连接数据库、注册定时任务。
- `server/src/app.js`：配置 Express、CORS、Helmet、JSON 解析、静态文件、统一路由、错误处理。
- `server/src/routes/index.js`：总路由入口。
- `server/src/middleware/auth.js`：JWT 签发、校验、refreshToken 校验。
- `server/src/middleware/rbac.js`：RBAC 权限校验。
- `server/src/middleware/audit.js`：操作日志记录。
- `server/src/middleware/rateLimit.js`：登录限流。
- `server/src/middleware/errorHandler.js`：统一错误处理。
- `server/src/models/index.js`：模型关联关系。
- `server/src/config/db.js`：Sequelize 连接 MySQL。

### 4. 数据库目录

```text
db/
├─ schema.sql              初始化建表脚本
└─ migrations/             后续迁移脚本
```

数据库核心表：

- 用户权限：`users`、`roles`、`permissions`、`role_permissions`
- 会员会籍：`members`、`body_measurements`、`membership_plans`、`memberships`
- 课程预约：`coaches`、`courses`、`course_schedules`、`bookings`、`course_reviews`
- 商品订单：`products`、`stock_movements`、`orders`、`order_items`、`payments`
- 系统能力：`audit_logs`、`notifications`、`check_ins`、`settings`

---

## 四、重点技术栈知识点

### 1. React 18

你需要熟悉：

- 函数组件
- Hooks，例如 `useState`、`useMemo`
- 组件拆分
- 页面组件和通用组件的区别
- 条件渲染
- 表单、弹窗、表格、分页等后台管理系统常见写法

项目体现：

- `pages/` 下每个业务页面都是 React 组件。
- `MainLayout` 实现后台布局。
- `NotificationBell`、`ChangePasswordModal` 等是可复用组件。

面试回答：

> 前端采用 React 函数组件开发，通过页面组件承载业务，通过通用组件抽离重复 UI。后台系统中大量使用表格、表单、抽屉、弹窗等交互组件，并通过状态控制弹窗显示、列表刷新和数据提交。

### 2. TypeScript

你需要熟悉：

- 接口类型定义
- 请求参数类型
- 响应数据类型
- 组件 props 类型
- 类型约束带来的开发安全性

项目体现：

- `client/src/api/member.ts` 定义了 `Member`、`MembershipPlan`、`MembershipCard` 等接口。
- `client/src/api/auth.ts` 定义了登录参数、用户信息和登录结果类型。

面试回答：

> 前端使用 TypeScript 对 API 参数、响应结果和页面数据进行类型约束，可以减少字段拼错、接口返回值不明确等问题，提高项目可维护性。

### 3. Vite

你需要熟悉：

- 快速开发服务器
- 前端打包
- 路径别名 `@`
- 开发环境代理

项目体现：

- `vite.config.ts` 中配置了 `/api` 代理到 `http://localhost:4000`。
- 前端开发端口是 `5173`。

面试回答：

> 项目前端使用 Vite 作为构建工具，开发环境启动快，并通过 proxy 把 `/api` 请求转发到后端，解决开发环境跨域和接口联调问题。

### 4. Ant Design

你需要熟悉：

- `Table`
- `Form`
- `Modal`
- `Drawer`
- `Menu`
- `Layout`
- `Message`
- `Upload`

项目体现：

- 后台布局使用 `Layout`、`Menu`。
- 会员、课程、订单等模块大量使用表格和表单。
- 操作成功/失败使用 `message` 提示。

面试回答：

> 项目使用 Ant Design 快速搭建后台管理界面，适合管理系统中的表格、表单、弹窗、抽屉、菜单和布局场景，提高了开发效率和界面一致性。

### 5. React Router

你需要熟悉：

- 路由配置
- 嵌套路由
- 路由守卫
- 重定向
- 404 / 403 页面

项目体现：

- `router/index.tsx` 配置了登录页、主布局页、会员页、课程页、财务页、系统页等。
- `RequireAuth` 判断是否登录。
- `RequirePermission` 判断是否有权限。

面试回答：

> 前端路由使用 React Router 进行页面管理，通过 `RequireAuth` 实现登录守卫，通过 `RequirePermission` 实现页面级权限控制。如果用户未登录会跳转登录页，没有权限会跳转 403 页面。

### 6. Zustand

你需要熟悉：

- 轻量状态管理
- 登录状态持久化
- 权限判断函数

项目体现：

- `authStore.ts` 保存 token、refreshToken、user。
- 使用 `persist` 把登录状态持久化到本地。
- `hasPermission` 根据权限码判断用户是否有权限。

面试回答：

> 项目使用 Zustand 管理全局登录状态，包括 token、refreshToken、用户信息和权限列表，同时使用 persist 做本地持久化，页面刷新后仍能保留登录状态。

### 7. Axios

你需要熟悉：

- 创建 Axios 实例
- 请求拦截器
- 响应拦截器
- token 自动携带
- 统一错误处理
- refreshToken 自动续期

项目体现：

- `request.ts` 中统一设置 `baseURL: /api`。
- 请求时自动带 `Authorization: Bearer token`。
- 响应时统一解析 `{ code, message, data }`。
- 遇到 401 时尝试调用 `/auth/refresh` 刷新 token。

面试回答：

> 我对 Axios 做了统一封装。请求拦截器负责自动携带 token，响应拦截器负责统一解析后端响应结构，并在 token 失效时通过 refreshToken 自动刷新登录状态。如果刷新失败，就清空登录状态并跳转登录页。

### 8. Node.js + Express

你需要熟悉：

- Express 应用创建
- 中间件机制
- 路由拆分
- Controller 分层
- RESTful API
- 统一错误处理

项目体现：

- `app.js` 配置全局中间件。
- `routes/` 按模块拆分路由。
- `controllers/` 按业务拆分处理函数。
- `middleware/` 实现鉴权、权限、审计、限流、错误处理。

面试回答：

> 后端使用 Express 搭建 RESTful API 服务，通过路由层负责接口分发，Controller 层负责业务逻辑，中间件负责鉴权、权限、审计日志、限流和错误处理。这种结构职责清晰，方便维护和扩展。

### 9. Sequelize ORM

你需要熟悉：

- 模型定义
- 表关系映射
- `findAll`、`findByPk`、`create`、`update`、`destroy`
- `include` 关联查询
- 事务 transaction
- 行锁 lock

项目体现：

- `models/index.js` 定义了用户、角色、权限、会员、会员卡、课程、预约、订单等关系。
- 预约和收银场景使用事务和行锁保证数据一致性。

面试回答：

> 后端通过 Sequelize 操作 MySQL，每张表对应一个模型，模型之间通过 `belongsTo`、`hasMany`、`belongsToMany` 建立关联。复杂业务比如预约课程和收银结账使用事务处理，避免库存、余额、预约人数等数据不一致。

### 10. MySQL

你需要熟悉：

- 表结构设计
- 主键、唯一索引、普通索引
- 一对多、多对多关系
- 订单、订单明细、支付流水设计
- 权限表设计

项目体现：

- `users` 和 `roles` 是多对一。
- `roles` 和 `permissions` 通过 `role_permissions` 多对多。
- `members` 和 `memberships` 是一对多。
- `orders` 和 `order_items`、`payments` 是一对多。

面试回答：

> 数据库采用 MySQL，核心设计包括 RBAC 权限模型、会员会籍模型、课程预约模型、商品库存模型和订单支付模型。通过主键、唯一索引和业务字段索引保证查询效率和数据约束。

### 11. JWT + Refresh Token

你需要熟悉：

- JWT 是什么
- token 放在哪里
- 请求时如何传递 token
- token 过期怎么办
- refreshToken 的作用

项目体现：

- 登录成功后后端返回 `token` 和 `refreshToken`。
- 前端请求接口时自动带 token。
- token 失效时前端调用 `/api/auth/refresh`。

面试回答：

> 登录成功后，后端生成短期 token 和 refreshToken。前端后续请求会在请求头中带上 token，后端通过 JWT 校验用户身份。如果 token 过期，前端会用 refreshToken 换取新的 token，从而减少用户频繁登录。

### 12. RBAC 权限模型

你需要熟悉：

- 用户、角色、权限三者关系
- 页面级权限
- 按钮级权限
- 接口级权限

项目体现：

- 数据库有 `users`、`roles`、`permissions`、`role_permissions`。
- 前端菜单通过权限码过滤。
- 前端路由通过 `RequirePermission` 控制访问。
- 后端接口通过 `requirePermission` 控制访问。

面试回答：

> 项目采用 RBAC 权限模型。用户属于角色，角色拥有多个权限。前端根据权限控制菜单和页面访问，后端通过中间件控制接口访问。这样即使前端绕过按钮直接请求接口，后端也会再次校验权限，保证安全性。

### 13. Zod 参数校验

你需要熟悉：

- 后端为什么要做参数校验
- 字段类型校验
- 必填项校验
- 错误提示返回

项目体现：

- 登录、会员保存、预约、收银等接口都使用 Zod 校验参数。

面试回答：

> 后端使用 Zod 对请求参数进行校验，例如新增会员时校验姓名不能为空、字段长度不能超限，收银时校验商品列表和支付金额。这样可以防止非法数据进入业务逻辑和数据库。

### 14. 事务和并发控制

你需要重点掌握，因为这是面试亮点。

项目中有两个典型场景：

- 课程预约
- 收银结账

课程预约中：

- 开启事务
- 查询课程排期时加行锁
- 在锁内判断容量是否已满
- 创建预约记录
- 增加已预约人数
- 提交事务

收银结账中：

- 开启事务
- 校验商品库存
- 扣减商品库存
- 校验储值卡余额
- 扣减储值卡余额
- 创建订单
- 创建订单明细
- 创建支付记录
- 如果购买会员卡，自动开卡
- 提交事务

面试回答：

> 对于容易出现并发问题的业务，我使用 Sequelize 事务和数据库行锁保证一致性。比如课程预约时，会对排期记录加锁，在锁内判断容量并更新已预约人数，避免多人同时预约导致超卖。收银结账时，把库存扣减、余额扣减、订单创建、支付记录和自动开卡放在同一个事务中，任何一步失败都会回滚。

### 15. ExcelJS 导入导出

你需要熟悉：

- Excel 读取
- Excel 写入
- 文件下载响应头
- 批量导入校验

项目体现：

- 会员 Excel 导入。
- 会员、订单、商品库存导出。

面试回答：

> 项目使用 ExcelJS 实现会员导入和数据导出。导入时读取 Excel 行数据，进行字段转换和手机号去重；导出时生成工作簿并设置响应头，让浏览器下载 Excel 文件。

### 16. Multer 文件上传

你需要熟悉：

- 文件上传中间件
- 文件大小限制
- 内存存储和磁盘存储

项目体现：

- 通用文件上传接口。
- 会员 Excel 导入使用 `multer.memoryStorage()` 读取文件 buffer。

面试回答：

> 文件上传使用 multer 处理，普通文件可以保存到 uploads 目录，Excel 导入则使用内存存储，直接读取上传文件的 buffer 给 ExcelJS 解析。

### 17. ECharts 数据看板

你需要熟悉：

- 图表数据来源
- 后端统计接口
- 前端图表展示

项目体现：

- 营收趋势
- 会员增长
- 热门课程 TOP5
- 库存预警

面试回答：

> 数据看板前端使用 ECharts 展示图表，后端提供统计接口，例如近 7 天营收趋势、会员近 6 个月增长、热门课程 TOP5 和库存预警。前端只负责展示，统计逻辑放在后端。

### 18. node-cron 定时任务

你需要熟悉：

- 定时任务用途
- 定时扫描
- 自动状态更新
- 通知生成

项目体现：

- 每天凌晨 2 点扫描过期会员卡。
- 启动时立即执行一次。
- 对即将到期的会员卡生成通知。

面试回答：

> 项目使用 node-cron 实现会员卡过期扫描任务。系统每天凌晨自动扫描已过期的会员卡并标记为 EXPIRED，同时对 7 天内即将过期的会员卡生成通知，提醒管理员或前台处理。

### 19. Docker Compose

你需要熟悉：

- 多服务编排
- 前端服务
- 后端服务
- MySQL 服务
- 服务依赖
- 数据卷

项目体现：

- `docker-compose.yml` 包含 `mysql`、`server`、`client` 三个服务。
- 前端通过 Nginx 提供静态资源并转发 `/api` 到后端。
- MySQL 使用 volume 持久化数据。

面试回答：

> 项目支持 Docker Compose 一键部署，包含 MySQL、后端 Node 服务和前端 Nginx 服务。MySQL 初始化时执行 schema.sql，后端依赖 MySQL 健康检查，前端 Nginx 负责静态资源和 API 反向代理。

### 20. Jest + Supertest

你需要熟悉：

- 冒烟测试
- 接口测试
- token 登录测试
- 受保护接口测试

项目体现：

- `server/tests/smoke.test.js` 测试健康检查、登录、会员列表、商品列表、报表、Dashboard 等接口。

面试回答：

> 后端使用 Jest 和 Supertest 做接口冒烟测试，主要验证核心接口是否可用，比如健康检查、登录、鉴权保护、会员列表、商品列表、报表和看板接口。

---

## 五、核心业务模块梳理

### 1. 登录与权限模块

涉及文件：

- 前端：`api/auth.ts`、`stores/authStore.ts`、`router/guards.tsx`
- 后端：`auth.routes.js`、`authController.js`、`auth.js`、`rbac.js`
- 数据库：`users`、`roles`、`permissions`、`role_permissions`

流程：

```text
用户输入账号密码
  ↓
前端调用 /api/auth/login
  ↓
后端校验用户名、密码 bcrypt
  ↓
生成 token 和 refreshToken
  ↓
查询角色权限列表
  ↓
返回用户信息和权限码
  ↓
前端保存到 Zustand
  ↓
菜单、路由、按钮根据权限控制展示
```

你要能说清：

- 密码不是明文保存，而是 bcrypt 哈希。
- token 用于接口鉴权。
- refreshToken 用于 token 过期后续期。
- 前后端都做权限控制，后端是最终安全边界。

### 2. 会员与会籍模块

涉及功能：

- 会员 CRUD
- 会员标签
- 体测记录
- 卡种模板
- 办卡
- 续费
- 挂起
- 恢复
- 转让
- 作废
- 即将过期提醒
- 批量续费
- 储值卡消费明细

涉及表：

- `members`
- `body_measurements`
- `membership_plans`
- `memberships`

面试重点：

> 会员是基础档案，会员卡是会员购买某个卡种后的实例。卡种模板定义价格、类型、有效期、次数或初始余额；会员卡记录具体会员的卡号、开始时间、结束时间、剩余次数、余额和状态。

### 3. 课程与预约模块

涉及功能：

- 教练管理
- 课程库
- 课程排期
- 预约课程
- 取消预约
- 预约签到
- 未到标记
- 会员自助预约
- 课程评价
- 教练绩效排行

涉及表：

- `coaches`
- `courses`
- `course_schedules`
- `bookings`
- `course_reviews`

面试重点：

> 课程库定义课程本身，课程排期定义某节课在什么时间、哪个教练、哪个地点、容量是多少。预约表记录会员预约某个排期。预约时使用事务和行锁锁住排期记录，在锁内判断容量并更新预约人数，避免并发超约。

### 4. 入场签到模块

涉及功能：

- 前台手动签到
- 二维码签到
- 微信绑定后扫码签到
- 今日签到统计
- 签到记录查询

涉及表：

- `check_ins`
- `members`
- `users`

面试重点：

> 签到记录单独建表，记录会员、签到时间、签到方式、操作人等信息。这样可以独立统计每天入场人数，也不会和课程预约签到混在一起。

### 5. 商品库存模块

涉及功能：

- 商品 CRUD
- 入库
- 出库
- 库存流水
- 库存预警
- 商品导出

涉及表：

- `products`
- `stock_movements`

面试重点：

> 商品表保存当前库存，库存流水表记录每一次库存变化。这样既能快速查询当前库存，也能追踪库存变动历史。

### 6. 收银订单模块

涉及功能：

- 商品购买
- 办卡购买
- 私教购买
- 多支付方式
- 储值卡支付
- 待支付订单
- 模拟支付确认
- 订单列表
- 订单详情
- 退款

涉及表：

- `orders`
- `order_items`
- `payments`
- `products`
- `memberships`

面试重点：

> 订单采用主从表设计，订单主表保存订单号、会员、总金额、状态；订单明细表保存商品、卡种或私教课的购买明细；支付表保存现金、微信、支付宝、银行卡、储值卡等支付流水。收银过程使用事务保证库存、余额、订单和支付记录一致。

### 7. 财务报表和数据看板

涉及功能：

- 日报
- 月报
- 支付方式汇总
- 每日营收趋势
- 会员增长趋势
- 热门课程 TOP5
- 库存预警

涉及技术：

- Sequelize 聚合查询
- dayjs 日期处理
- ECharts 图表展示

面试重点：

> 报表模块的核心是从订单、支付、会员、预约和库存等表中做统计聚合。后端负责计算统计结果，前端使用 ECharts 展示趋势图、柱状图或列表。

### 8. 系统管理模块

涉及功能：

- 用户管理
- 角色管理
- 权限配置
- 操作日志
- 系统设置

涉及表：

- `users`
- `roles`
- `permissions`
- `role_permissions`
- `audit_logs`
- `settings`

面试重点：

> 系统管理模块支撑整个平台的安全和配置能力。用户和角色决定谁能登录，权限决定能访问哪些页面和接口，操作日志用于追踪关键操作。

---

## 六、最重要的代码调用链

### 1. 新增会员调用链

```text
Members 页面提交表单
  ↓
apiMemberCreate
  ↓
request.post('/members')
  ↓
Axios 请求拦截器自动带 token
  ↓
Express /api/members
  ↓
authRequired 登录校验
  ↓
requirePermission('member:create') 权限校验
  ↓
audit('member', 'create') 记录操作日志
  ↓
memberController.createMember
  ↓
Zod 校验参数
  ↓
生成会员编号
  ↓
Member.create 写入 members 表
  ↓
返回 { code: 0, message: '已创建', data }
  ↓
前端提示成功并刷新列表
```

### 2. 登录调用链

```text
Login 页面提交账号密码
  ↓
apiLogin
  ↓
POST /api/auth/login
  ↓
rateLimit 登录限流
  ↓
authController.login
  ↓
查询用户和角色
  ↓
bcrypt.compare 校验密码
  ↓
生成 token 和 refreshToken
  ↓
查询角色权限
  ↓
返回用户信息和权限列表
  ↓
Zustand 保存登录态
  ↓
跳转后台首页
```

### 3. 课程预约调用链

```text
前端提交预约
  ↓
POST /api/bookings
  ↓
登录校验 + 权限校验
  ↓
bookingController.createBooking
  ↓
开启事务
  ↓
锁定 course_schedules 排期行
  ↓
检查排期状态、时间、容量
  ↓
检查会员是否重复预约
  ↓
创建 booking 记录
  ↓
更新 booked_count
  ↓
提交事务
  ↓
返回预约成功
```

### 4. 收银结账调用链

```text
收银台选择商品/卡种/私教课
  ↓
提交支付方式和金额
  ↓
POST /api/orders/checkout
  ↓
登录校验 + order:manage 权限校验
  ↓
orderController.checkout
  ↓
开启事务
  ↓
校验商品、卡种、课程是否可用
  ↓
商品加锁并扣库存
  ↓
校验支付金额是否等于应付金额
  ↓
储值卡支付时加锁并扣余额
  ↓
创建订单主表
  ↓
创建订单明细
  ↓
创建支付流水
  ↓
如果购买卡种则自动开会员卡
  ↓
提交事务
  ↓
返回收银成功
```

---

## 七、可以写到简历上的项目描述

### 版本一：简洁版

**健身房管理系统**

基于 React、TypeScript、Ant Design、Node.js、Express、Sequelize 和 MySQL 开发的前后端分离管理系统，支持会员管理、会籍办卡、课程预约、教练排课、商品库存、收银订单、财务报表、数据看板和系统权限管理。项目实现 JWT 登录认证、RBAC 权限控制、Axios 请求封装、Sequelize 事务处理、Excel 导入导出和 Docker Compose 部署。

### 版本二：偏技术版

**健身房管理系统 | React + Express + MySQL**

- 使用 React 18、TypeScript、Vite、Ant Design 搭建后台管理前端，封装 Axios 请求拦截器，实现 token 自动携带、统一响应处理和 refreshToken 自动续期。
- 使用 Zustand 管理登录态和权限信息，结合 React Router 实现登录守卫和页面级权限控制。
- 使用 Node.js + Express 搭建 RESTful API，按认证、会员、课程、财务、系统等模块拆分路由和控制器。
- 基于 JWT 和 RBAC 实现用户、角色、权限管理，支持菜单级、页面级和接口级权限控制。
- 使用 Sequelize ORM 操作 MySQL，设计会员、会籍、课程排期、预约、商品、订单、支付等核心数据模型。
- 在课程预约和收银结账场景中使用事务和行锁，保证预约容量、库存扣减、储值余额和订单支付数据一致性。
- 使用 ExcelJS 实现会员导入、会员/订单/商品导出，使用 node-cron 实现会员卡过期扫描和到期通知。
- 使用 ECharts 实现营收趋势、会员增长、热门课程和库存预警等数据看板，支持 Docker Compose 一键部署。

### 版本三：偏业务成果版

**健身房综合运营管理系统**

负责设计并实现一个覆盖健身房核心业务流程的管理系统，包括会员档案、卡种会籍、课程预约、教练排课、入场签到、商品库存、收银订单、退款、财务报表、通知中心和权限管理。系统采用前后端分离架构，前端基于 React + TypeScript + Ant Design，后端基于 Express + Sequelize + MySQL。通过 RBAC 权限模型保障系统安全，通过事务和行锁解决预约超额、库存扣减和储值卡余额扣减的一致性问题，并支持 Excel 导入导出、定时任务和 Docker 部署。

---

## 八、简历项目亮点怎么写

建议写 5 到 7 条，不要太多。

可以这样写：

- **前后端分离架构**：前端使用 React + TypeScript + Ant Design，后端使用 Express + Sequelize，接口采用 RESTful 风格。
- **登录与权限体系**：基于 JWT + refreshToken 实现登录认证，基于 RBAC 实现角色权限控制，支持菜单、路由、接口多层权限校验。
- **复杂业务建模**：设计会员、会籍、课程排期、预约、商品库存、订单支付等数据模型，覆盖健身房主要业务流程。
- **事务一致性保障**：在课程预约、收银结账、库存扣减、储值卡支付等场景中使用 Sequelize 事务和行锁，避免并发数据不一致。
- **数据统计与可视化**：基于订单、会员、预约、库存数据实现营收趋势、会员增长、热门课程和库存预警看板。
- **工程化能力**：封装 Axios 请求层、统一错误响应、操作审计日志、登录限流、Excel 导入导出、Docker Compose 部署和接口冒烟测试。

---

## 九、面试官高频问题与标准回答

### 1. 这个项目的技术架构是什么？

回答：

> 项目采用前后端分离架构。前端使用 React 18、Vite、TypeScript、Ant Design、React Router 和 Zustand，负责页面展示、路由、状态管理和接口调用。后端使用 Node.js、Express、Sequelize 和 MySQL，负责接口服务、业务逻辑、登录鉴权、权限控制和数据持久化。前端通过 Axios 调用 `/api` 接口，开发环境由 Vite proxy 转发到后端，生产环境由 Nginx 转发到后端。部署上使用 Docker Compose 编排前端、后端和 MySQL 三个服务。

### 2. 为什么做前后端分离？

回答：

> 前后端分离可以让页面展示和业务接口解耦。前端专注用户体验和交互，后端专注业务逻辑、权限和数据安全。这样前端和后端可以独立开发、独立测试、独立部署，也方便后续扩展移动端或小程序端。

### 3. 权限系统怎么设计的？

回答：

> 权限系统采用 RBAC 模型，也就是用户、角色、权限三层结构。用户表关联角色表，角色通过中间表关联多个权限。用户登录后，后端返回角色和权限码列表，前端根据权限码控制菜单和路由，后端接口通过 `requirePermission` 中间件再次校验权限。这样即使用户绕过前端按钮直接请求接口，后端也能拦截，安全性更高。

### 4. JWT 登录流程是什么？

回答：

> 用户登录时，前端把账号密码传给后端，后端查询用户并用 bcrypt 校验密码。校验通过后，后端生成 token 和 refreshToken，并返回用户信息和权限列表。前端保存到 Zustand 中，后续请求通过 Axios 请求拦截器自动把 token 放到 Authorization 请求头。后端通过 JWT 校验 token，有效则放行，无效则返回 401。前端遇到 401 时会尝试用 refreshToken 刷新 token。

### 5. 课程预约怎么防止超额预约？

回答：

> 课程预约是一个典型并发场景。我的做法是开启数据库事务，然后对课程排期记录加行锁，在锁内判断课程是否开放、是否已开始、预约人数是否达到容量。如果没满，就创建预约记录并更新 booked_count。整个过程在一个事务里完成，失败就回滚，从而避免多人同时预约导致超额。

### 6. 收银结账怎么保证数据一致性？

回答：

> 收银涉及订单、订单明细、支付流水、商品库存、储值卡余额、自动开卡等多个表。项目中把这些操作放在同一个事务里处理。如果是商品购买，会先锁定商品并扣库存；如果是储值卡支付，会锁定会员卡并扣余额；然后创建订单、明细和支付记录；如果购买的是会员卡，还会自动生成会员卡。只要其中任意一步失败，整个事务都会回滚。

### 7. 数据看板是怎么实现的？

回答：

> 数据看板由后端提供统计接口，前端使用 ECharts 展示。比如营收趋势是后端按日期统计已支付订单金额，会员增长是按月份统计新会员数量，热门课程是根据预约表聚合统计，库存预警是查询库存低于预警线的商品。前端只负责调用接口并渲染图表。

### 8. Excel 导入导出怎么做？

回答：

> 项目使用 ExcelJS 实现导入导出。导入会员时，后端通过 multer 接收 Excel 文件，ExcelJS 读取 buffer 并解析每一行，做字段转换、手机号去重和会员编号生成，然后批量写入数据库。导出时，后端查询数据并生成工作簿，设置下载响应头，浏览器就可以下载 Excel 文件。

### 9. 操作日志怎么实现？

回答：

> 后端封装了 audit 中间件，在关键接口上使用，比如新增会员、修改角色、收银、退款等。接口响应成功后，中间件异步写入操作日志，记录操作人、模块、动作、请求路径、参数、IP 和耗时。这样可以追踪后台关键操作。

### 10. 项目怎么部署？

回答：

> 项目支持 Docker Compose 部署，包括 MySQL、后端 server 和前端 client 三个服务。MySQL 使用数据卷持久化，并在初始化时执行 schema.sql。后端连接 MySQL 并提供 4000 端口 API。前端构建后由 Nginx 提供静态资源，并把 `/api` 请求反向代理到后端服务。

---

## 十、你必须重点掌握的 10 个面试点

### 1. 项目架构

要能说出：

```text
React 前端 + Express 后端 + MySQL 数据库 + Docker Compose 部署
```

并说明各层职责。

### 2. 前端请求封装

要能说出：

- Axios 统一 `baseURL`
- 请求拦截器带 token
- 响应拦截器解析统一返回
- 401 自动 refreshToken
- refresh 失败跳登录页

### 3. Zustand 登录状态

要能说出：

- 保存 token、refreshToken、user
- persist 持久化
- hasPermission 判断权限

### 4. 路由权限控制

要能说出：

- `RequireAuth` 控制是否登录
- `RequirePermission` 控制页面权限
- 无权限跳 403

### 5. RBAC 权限

要能说出：

- 用户属于角色
- 角色拥有权限
- 权限控制菜单、页面、接口
- 后端接口校验才是安全边界

### 6. Sequelize 模型关系

要能说出：

- 会员与会员卡是一对多
- 卡种与会员卡是一对多
- 课程与排期是一对多
- 排期与预约是一对多
- 订单与明细、支付是一对多
- 角色与权限是多对多

### 7. 事务和行锁

必须重点掌握：

- 课程预约防超额
- 收银结账防库存、余额、订单不一致

### 8. 数据统计

要能说出：

- 后端聚合统计
- 前端 ECharts 展示
- 统计来源于订单、会员、预约、库存表

### 9. Excel 导入导出

要能说出：

- multer 接文件
- ExcelJS 解析/生成文件
- 导入校验
- 导出设置响应头

### 10. Docker 部署

要能说出：

- client 前端 Nginx
- server 后端 Node
- mysql 数据库
- Nginx 转发 `/api`
- MySQL volume 持久化

---

## 十一、学习和复习路线

### 第一天：先掌握整体架构

重点看：

- `README.md`
- `docker-compose.yml`
- `client/src/router/index.tsx`
- `server/src/app.js`
- `server/src/routes/index.js`
- `server/src/models/index.js`
- `db/schema.sql`

目标：能讲清前端、后端、数据库怎么协作。

### 第二天：掌握登录和权限

重点看：

- `client/src/api/request.ts`
- `client/src/stores/authStore.ts`
- `client/src/router/guards.tsx`
- `server/src/controllers/authController.js`
- `server/src/middleware/auth.js`
- `server/src/middleware/rbac.js`

目标：能讲清 JWT、refreshToken、RBAC。

### 第三天：掌握会员和会籍

重点看：

- `client/src/api/member.ts`
- `server/src/routes/member.routes.js`
- `server/src/controllers/memberController.js`
- `server/src/controllers/membershipController.js`
- `Member`、`Membership`、`MembershipPlan` 模型

目标：能讲清会员、卡种、会员卡的关系。

### 第四天：掌握课程预约

重点看：

- `server/src/routes/course.routes.js`
- `server/src/controllers/bookingController.js`
- `Course`、`CourseSchedule`、`Booking` 模型

目标：能讲清课程预约如何防并发超约。

### 第五天：掌握收银订单

重点看：

- `server/src/routes/finance.routes.js`
- `server/src/controllers/orderController.js`
- `Product`、`Order`、`OrderItem`、`Payment` 模型

目标：能讲清收银事务、库存扣减、储值卡支付、自动开卡。

### 第六天：掌握报表、导入导出、定时任务

重点看：

- `dashboardController.js`
- `reportController.js`
- `importController.js`
- `exportController.js`
- `jobs/expireMemberships.js`

目标：能讲清统计、Excel、定时任务。

### 第七天：准备简历和模拟面试

重点准备：

- 项目一句话介绍
- 技术架构回答
- 3 个项目亮点
- 2 个难点解决方案
- 1 个业务数据流例子

---

## 十二、面试时推荐你的项目介绍顺序

不要一上来讲代码细节。建议按这个顺序：

```text
1. 项目背景
2. 技术架构
3. 核心业务模块
4. 我负责/实现的重点
5. 技术难点
6. 项目亮点
7. 可以扩展优化的地方
```

可以这样完整表达：

> 这个项目是一个健身房综合管理系统，主要解决健身房在会员、会籍、课程、预约、库存、收银和财务统计方面的管理问题。项目采用前后端分离架构，前端用 React、TypeScript、Ant Design，后端用 Express、Sequelize，数据库用 MySQL。
>
> 前端主要负责后台管理页面、路由、权限菜单和接口调用；后端负责登录鉴权、权限校验、业务逻辑和数据库操作；数据库负责保存会员、课程、订单、支付、库存和权限等数据。
>
> 我比较重点实现和理解的是 JWT 登录、RBAC 权限、会员会籍、课程预约、收银订单和数据看板。其中课程预约和收银结账涉及并发和数据一致性，所以我使用 Sequelize 的事务和行锁来保证预约容量、库存、余额和订单数据的一致。

---

## 十三、项目优化方向（面试官最在意的 8 大优化点）

面试官问"项目还有什么可以优化"时，他真正想考察的是你是否具备**线上系统的工程思维**：性能、安全、可靠性、可扩展性。下面按面试官关注度从高到低排列，每个优化点都给出：现状问题 → 优化方案 → 面试回答话术。

---

### 优化点 1：数据库查询性能优化（面试官最常问）

**现状问题**：

- 会员列表、订单列表等接口使用 `findAll + include` 做关联查询，数据量大了以后会出现慢查询。
- 报表统计接口每次都实时聚合 `orders`、`payments` 等大表，并发访问时数据库压力大。
- 部分查询字段没有建索引，例如 `members.phone`、`orders.status`、`bookings.schedule_id`。

**优化方案**：

- **加索引**：对高频查询条件和关联字段加普通索引，对手机号等唯一字段加唯一索引。
- **分页优化**：列表接口全部使用 `findAndCountAll` 的 `offset/limit` 分页，避免一次查全量；深度分页可以改用游标分页（基于 ID 的 `WHERE id > lastId LIMIT N`）。
- **统计宽表**：为日报/月报建一张 `daily_revenue_summary` 汇总表，由定时任务每天凌晨写入，报表接口直接查汇总表而不是实时聚合。
- **查询缓存**：看板数据变化不频繁，可以在后端加 5 分钟内存缓存或 Redis 缓存，减少数据库查询次数。
- **避免 N+1 查询**：Sequelize 的 `include` 默认会生成多次查询，可以改用 `separate: false` 或手写 SQL JOIN。

**面试回答**：

> 目前列表查询使用 Sequelize 的 include 做关联，数据量小的时候没问题，但数据量大了以后会出现慢查询。优化方向有三个：一是给高频查询字段加索引；二是报表统计改为读预计算的汇总宽表，而不是每次实时聚合；三是看板数据加缓存，因为看板数据不需要实时刷新，5 分钟缓存就可以大幅减少数据库压力。另外深度分页可以改用游标分页，避免 OFFSET 扫描大量行。

---

### 优化点 2：缓存层引入 Redis（面试官第二常问）

**现状问题**：

- 权限缓存存在 Node.js 进程内存中，多实例部署时各实例缓存不共享，权限修改后其他实例不会立即生效。
- 登录限流计数器存在内存中，多实例部署时限流失效，用户可以通过不同实例绕过限制。
- refreshToken 存在数据库中，每次刷新都要查库。

**优化方案**：

- **权限缓存 → Redis**：用户登录后把权限列表存 Redis（key = `perm:{userId}`，TTL 30 分钟），管理员修改权限后删除对应 key，下次请求自动从数据库重新加载。
- **限流计数器 → Redis**：使用 Redis 的 `INCR + EXPIRE` 实现滑动窗口限流，天然支持多实例。
- **refreshToken → Redis**：token 失效时直接从 Redis 取 refreshToken 验证，避免查数据库。同时可以在 Redis 中主动让某个 token 失效（比如用户修改密码后踢掉旧 token）。
- **看板数据 → Redis**：营收趋势、热门课程等接口结果缓存 5 分钟，减少数据库聚合查询。

**面试回答**：

> 目前权限缓存和限流计数器都存在 Node 进程内存中，单实例没问题，但多实例部署时会不一致。优化方案是引入 Redis：权限缓存存 Redis，修改权限后删 key 让缓存失效；限流用 Redis 的 INCR 原子操作，天然支持分布式；refreshToken 也存 Redis，验证时不用查库，还能在用户改密码后主动让旧 token 失效。

---

### 优化点 3：接口安全加固（面试官很关注安全意识）

**现状问题**：

- JWT secret 是硬编码的 `change-me-in-production`，生产环境需要从环境变量注入强密钥。
- 没有对用户输入做 XSS 过滤，前端展示时如果用 `dangerouslySetInnerHTML` 可能被注入。
- 文件上传没有做文件类型白名单校验，理论上可以上传 .html 或 .exe 文件。
- 没有做 CSRF 防护（虽然 JWT 不依赖 Cookie，但如果有 Cookie 场景仍需注意）。
- 部分接口只做了权限码校验，没有做数据归属校验（例如某个前台能否查看其他门店的会员）。

**优化方案**：

- **JWT secret 环境变量化**：生产环境必须通过 `process.env.JWT_SECRET` 注入，且定期轮换。
- **XSS 防护**：后端对所有用户输入做 HTML 转义（可以使用 `xss` 或 `dompurify` 库），前端避免使用 `dangerouslySetInnerHTML`。
- **文件上传白名单**：multer 配置中增加 `fileFilter`，只允许 `.jpg`、`.png`、`.xlsx` 等指定扩展名。
- **接口响应头加固**：Helmet 已经启用了 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY` 等，可以再增加 `Content-Security-Policy`。
- **数据归属校验**：在 Controller 中增加门店/机构维度的数据过滤，确保用户只能访问自己管辖范围的数据。

**面试回答**：

> 安全方面有几个可以加固的地方：一是 JWT secret 目前是硬编码的，生产环境必须从环境变量注入强密钥；二是文件上传没有做类型白名单，需要加 fileFilter 只允许指定扩展名；三是用户输入没有做 XSS 过滤，可以在后端统一转义；四是部分接口缺少数据归属校验，比如前台应该只能看自己门店的会员，需要加机构维度过滤。Helmet 已经帮我们加了一些安全响应头，但还可以补充 CSP 策略。

---

### 优化点 4：事务与并发场景的进一步加固

**现状问题**：

- 课程预约使用行锁 `SELECT ... FOR UPDATE`，高并发下同一排期的锁竞争会导致请求排队，响应变慢。
- 收银结账事务涉及多张表，事务持有时间较长，锁冲突概率增大。
- 没有做乐观锁重试机制，锁等待超时后直接报错。

**优化方案**：

- **预约场景引入乐观锁**：在 `course_schedules` 表加 `version` 字段，预约时先读出版本号，更新时带 `WHERE version = 旧版本号`，如果影响行数为 0 说明被别人改了，自动重试 2~3 次。乐观锁适合读多写少、冲突不频繁的场景。
- **收银场景拆分事务**：把"校验+预留"和"确认支付"拆成两步。先在一个短事务中锁定商品并扣库存、锁定储值卡并扣余额，状态标记为"待确认"；支付回调到达后再用另一个短事务确认订单。这样减少长事务的锁持有时间。
- **增加锁等待超时配置**：MySQL 的 `innodb_lock_wait_timeout` 默认 50 秒，可以调低到 5 秒，避免一个慢事务卡住其他请求。
- **增加并发重试**：对于乐观锁冲突或锁超时，可以在 Controller 中加自动重试逻辑（最多 3 次），而不是直接返回错误。

**面试回答**：

> 目前课程预约用的是悲观锁（SELECT FOR UPDATE），高并发时锁竞争会导致请求排队。优化方向是引入乐观锁：在排期表加 version 字段，更新时带版本号判断，冲突时自动重试。收银结账目前是一个大事务，锁持有时间长，可以拆成"预留"和"确认"两步短事务，减少锁冲突。另外可以调低 MySQL 锁等待超时时间，避免一个慢事务卡住整个系统。

---

### 优化点 5：前端性能优化

**现状问题**：

- 所有页面组件都在主 bundle 中，没有按路由懒加载，首屏加载慢。
- 表格数据没有做虚拟滚动，大数据量时 DOM 节点多，渲染卡顿。
- 没有做接口请求的防抖/节流，快速操作会发大量重复请求。

**优化方案**：

- **路由懒加载**：使用 `React.lazy(() => import('./pages/Members'))` + `Suspense`，每个路由页面独立打包，首屏只加载登录页和当前页。
- **表格虚拟滚动**：引入 `react-window` 或 `react-virtualized`，只渲染可视区域的行，大数据量时性能提升明显。
- **请求防抖**：搜索输入框加 `debounce`（300ms），避免每输入一个字就发一次请求。
- **组件级代码分割**：ECharts 图表组件用 `lazy` 加载，因为 ECharts 体积大，只在看板页才需要。
- **静态资源 CDN**：生产环境把 React、Ant Design、ECharts 等大依赖从 CDN 加载，减小 bundle 体积。

**面试回答**：

> 前端性能优化主要有几个方向：一是路由懒加载，用 React.lazy 把每个页面独立打包，首屏只加载当前路由的代码；二是大数据表格用虚拟滚动，只渲染可视区域的行；三是搜索输入加防抖，避免频繁请求；四是 ECharts 等大体积依赖做组件级懒加载或走 CDN，减小主 bundle 体积。

---

### 优化点 6：日志监控与可观测性

**现状问题**：

- 后端使用 Winston 写文件日志，但日志分散在容器内，没有集中管理。
- 没有应用性能监控（APM），接口变慢时无法定位是数据库慢还是业务逻辑慢。
- 没有告警机制，服务挂了只能靠用户反馈。

**优化方案**：

- **日志集中管理**：用 Filebeat 采集容器日志，发送到 ELK（Elasticsearch + Logstash + Kibana），支持搜索和可视化分析。
- **APM 接入**：接入 Prometheus + Grafana 监控接口响应时间、错误率、数据库连接池使用率等指标。
- **告警规则**：配置 Grafana 告警，比如接口 P99 响应时间 > 2 秒、5 分钟内 500 错误 > 10 次、数据库连接池使用率 > 80% 时自动通知。
- **结构化日志**：Winston 日志改为 JSON 格式，方便 ELK 解析和检索。
- **链路追踪**：引入 OpenTelemetry，给每个请求生成 traceId，跨服务调用时可以追踪完整链路。

**面试回答**：

> 目前日志用 Winston 写文件，但分散在容器里不好查。优化方向是接入 ELK 做日志集中管理，配合 Prometheus + Grafana 做接口指标监控和告警。日志改为 JSON 格式方便检索，还可以引入 OpenTelemetry 做链路追踪，定位慢接口时能看出是数据库慢还是业务逻辑慢。

---

### 优化点 7：部署与运维优化

**现状问题**：

- Docker Compose 是单机部署，不支持弹性伸缩。
- 数据库和后端部署在同一台机器，数据库故障会影响整个系统。
- 没有健康检查自动恢复机制。
- 前端每次重新构建，没有利用缓存层。

**优化方案**：

- **数据库独立部署**：MySQL 使用云数据库（如 RDS），自带主从复制、自动备份和故障切换。
- **后端容器化编排升级**：从 Docker Compose 升级到 Kubernetes，支持自动扩缩容、滚动更新和故障自愈。
- **健康检查与自动重启**：Docker 配置 `healthcheck`，K8s 配置 `livenessProbe` + `readinessProbe`，进程异常时自动重启。
- **前端 CDN + 缓存**：静态资源上传 CDN，Nginx 配置 `Cache-Control` 缓存策略，HTML 不缓存，JS/CSS 用内容哈希做长期缓存。
- **CI/CD 流水线**：接入 GitHub Actions 或 GitLab CI，提交代码后自动测试、构建、部署，减少人工操作。

**面试回答**：

> 部署方面目前是 Docker Compose 单机部署，优化方向有几个：一是数据库迁移到云 RDS，自带主从和备份；二是后端从 Compose 升级到 K8s，支持自动扩缩容和滚动更新；三是前端静态资源走 CDN 并配置缓存策略；四是接入 CI/CD 流水线，实现自动化测试和部署。

---

### 优化点 8：代码质量与测试完善

**现状问题**：

- 目前只有冒烟测试（smoke test），覆盖了核心接口的可用性，但没有覆盖业务边界和异常分支。
- Controller 中业务逻辑较重，部分逻辑可以抽到 Service 层，提高可测试性。
- 没有前端测试。

**优化方案**：

- **Service 层抽离**：把 Controller 中的核心业务逻辑（如预约校验、收银计算）抽到 Service 层，Controller 只负责参数接收和响应返回。Service 层可以独立做单元测试。
- **后端单元测试**：用 Jest 对 Service 层写单元测试，Mock 掉 Sequelize 模型，专注测试业务逻辑。
- **后端集成测试**：用 Supertest 对完整接口写测试，覆盖正常流程、参数校验失败、权限不足、并发冲突等场景。
- **事务测试**：专门测试收银事务的回滚场景，比如库存不足时订单不应该创建、储值卡余额不足时支付不应该成功。
- **前端组件测试**：用 React Testing Library 测试关键页面组件的渲染和交互。

**面试回答**：

> 目前测试只有冒烟测试，覆盖了接口可用性但没覆盖业务边界。优化方向是：一是把 Controller 中的业务逻辑抽到 Service 层，提高可测试性；二是对 Service 层写单元测试，Mock 数据库专注测业务逻辑；三是用 Supertest 写集成测试，覆盖权限不足、参数校验失败、事务回滚等异常场景；四是前端用 React Testing Library 补充组件测试。

---

### 面试时如何组织优化回答

面试官问"项目还有什么可以优化"时，**不要一口气说 8 个点**，建议按以下策略：

1. **先说 2~3 个最核心的**：数据库查询优化 + Redis 缓存 + 事务并发加固，这三个是面试官最认可的。
2. **再说 1~2 个体现工程视野的**：日志监控 + 部署升级，说明你考虑过线上运维。
3. **如果面试官追问再展开**：安全加固、前端性能、测试完善。

**推荐回答模板**：

> 我觉得最需要优化的有三个方向。第一是数据库查询性能，目前报表接口每次实时聚合大表，数据量大了会很慢，可以建统计宽表加缓存。第二是引入 Redis 做缓存和限流，目前权限缓存和限流计数器都在进程内存里，多实例部署时不共享。第三是事务并发场景可以引入乐观锁，目前用的悲观锁在高并发时锁竞争严重，乐观锁加自动重试会更高效。另外在运维层面，可以接入 ELK 做日志集中管理，用 Prometheus + Grafana 做监控告警。

---

## 十四、最终总结话术

面试最后可以这样总结：

> 这个项目让我完整实践了一个后台管理系统从前端页面、接口设计、权限体系、数据库建模、复杂业务事务处理到部署测试的完整流程。技术上我重点掌握了 React + TypeScript 的后台开发、Express 分层架构、JWT/RBAC 权限体系、Sequelize ORM、MySQL 表关系设计、事务和行锁、Excel 导入导出、数据可视化和 Docker Compose 部署。业务上也理解了会员、会籍、预约、库存、收银、订单和财务报表这些真实管理系统中的核心流程。

---

## 十五、你背熟这 5 句话就能撑住大部分面试

1. **项目架构**：这是一个 React + Express + MySQL 的前后端分离健身房管理系统。
2. **权限设计**：系统使用 JWT 做登录认证，使用 RBAC 做用户、角色、权限控制，前后端都做权限校验。
3. **数据流**：前端通过 Axios 调用后端 API，后端经过鉴权、权限、参数校验后，用 Sequelize 操作 MySQL，再返回统一 JSON。
4. **核心难点**：课程预约和收银结账使用事务和行锁，保证预约人数、库存、余额、订单和支付记录一致。
5. **项目亮点**：系统覆盖会员、会籍、课程、预约、收银、库存、报表、看板、导入导出、定时任务和 Docker 部署，业务完整度比较高。
