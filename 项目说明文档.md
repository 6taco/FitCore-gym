# 健身房管理系统 — 项目说明文档

## 1. 项目概述

健身房管理系统是一个基于 React + Express + MySQL 的全栈 Web 应用，提供会员管理、会籍卡种、课程预约、商品库存、收银结账、财务报表、数据看板等完整的健身房运营管理功能。系统采用 RBAC 权限模型，支持管理员、前台、教练、会员四种角色，具备操作审计、定时任务、QR 签到、Excel 导入导出等企业级特性。

---

## 2. 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React + TypeScript | 18 / 5.4 |
| 构建工具 | Vite | 5.2 |
| UI 组件库 | Ant Design | 5.17 |
| 状态管理 | Zustand | 4.5 |
| 路由 | React Router | 6.23 |
| 图表 | ECharts | 5.5 |
| HTTP 客户端 | Axios | 1.7 |
| 后端框架 | Express | 4.19 |
| ORM | Sequelize | 6.37 |
| 数据库 | MySQL | 8.0+ |
| 认证 | JWT (jsonwebtoken) | 9.0 |
| 数据验证 | Zod | 3.23 |
| 日志 | Winston | 3.13 |
| 定时任务 | node-cron | 4.2 |
| Excel 处理 | exceljs | 4.4 |
| 文件上传 | multer | 1.4 |
| 测试 | Jest + Supertest | 30 / 7.2 |
| 部署 | Docker + Docker Compose + Nginx | - |

---

## 3. 项目结构

```
健身房管理系统/
├─ client/                        # 前端 (React + Vite + TypeScript)
│  ├─ src/
│  │  ├─ pages/                   # 页面组件
│  │  │  ├─ Dashboard/            # 数据看板
│  │  │  ├─ Members/              # 会员管理、体测、签到
│  │  │  ├─ Memberships/          # 会籍与卡种管理
│  │  │  ├─ Courses/              # 课程、排期、教练、预约
│  │  │  ├─ Finance/              # 收银台、订单、商品、报表
│  │  │  ├─ System/               # 用户、角色、权限、设置、日志
│  │  │  ├─ Login/                # 登录页
│  │  │  └─ Error/                # 错误页 (403/404)
│  │  ├─ api/                     # API 调用层
│  │  ├─ stores/                  # Zustand 状态管理
│  │  ├─ config/                  # 菜单配置、字典常量
│  │  ├─ router/                  # 路由与权限守卫
│  │  ├─ layouts/                 # 页面布局
│  │  └─ components/              # 公共组件
│  ├─ Dockerfile                  # 多阶段构建 (build + Nginx)
│  └─ nginx.conf                  # Nginx 反向代理配置
│
├─ server/                        # 后端 (Express + Sequelize)
│  ├─ src/
│  │  ├─ controllers/             # 业务逻辑 (20+ 控制器)
│  │  ├─ models/                  # Sequelize 模型 (21 张表)
│  │  ├─ routes/                  # API 路由定义
│  │  ├─ middleware/              # 认证、RBAC、审计、限流、错误处理
│  │  ├─ config/                  # 环境变量、数据库连接
│  │  ├─ constants/               # 权限字典
│  │  ├─ utils/                   # 响应格式、日志、ID 生成
│  │  ├─ jobs/                    # 定时任务 (会籍过期清理)
│  │  ├─ app.js                   # Express 应用配置
│  │  └─ index.js                 # 启动入口
│  ├─ scripts/
│  │  ├─ seed.js                  # 种子数据
│  │  └─ dbInit.js                # 数据库初始化
│  ├─ tests/                      # 单元/冒烟测试
│  └─ Dockerfile                  # Node 20 Alpine 构建
│
├─ db/
│  ├─ schema.sql                  # 完整建表脚本
│  └─ migrations/                 # 增量迁移 (6 个)
│
├─ docs/                          # 文档与路线图
├─ docker-compose.yml             # 一键部署
└─ package.json                   # Monorepo 根脚本
```

---

## 4. 功能模块

### 4.1 权限与认证 (RBAC)

- **四种角色**：管理员 (admin)、前台 (staff)、教练 (coach)、会员 (member)
- **按钮级权限控制**：后端 `requirePermission` 中间件 + 前端 `hasPermission` 判断
- **JWT 双 Token 机制**：accessToken (2h) + refreshToken (7d)，客户端自动刷新
- **操作审计**：自动记录所有变更操作（模块、操作类型、目标、IP、用户）

### 4.2 会员管理

- 会员档案 CRUD（编号、姓名、性别、生日、电话、身份证、头像、标签）
- 体测记录时间线（体脂率、肌肉量、BMI 追踪）
- 会员统计（新增趋势、活跃度、卡类分布）
- 会员搜索（名称、电话、编号）

### 4.3 会籍与卡种

- **三种卡类型**：
  - 期限卡 (PERIOD)：月卡、季卡、年卡，按天计有效期
  - 次卡 (COUNT)：固定次数，课程扣次制
  - 储值卡 (STORED)：余额制，支持充值
- **卡操作**：办卡、续费、挂起/恢复、转让、作废
- 自动过期清理（node-cron 定时任务）
- 储值卡交易流水查询

### 4.4 课程与预约

- 课程库管理（团课 / 私教）
- 排课周视图，实时显示容量（已报/总容量）
- 预约管理（预约、取消、签到、未签到）
- **并发安全**：课时扣减使用数据库行锁
- 教练业绩排名（课时数、营收、评价平均分）
- 课程评价（1-5 星评分 + 文字评价）

### 4.5 商品与库存

- 商品 CRUD（编码、名称、分类、价格、成本、库存、预警阈值）
- 库存流水（入库、出库、盘点），退款自动回退库存
- 库存预警（仪表板实时展示）

### 4.6 收银台与订单

- **混合结账**：商品 + 办卡/续费 + 课程，一单结账
- **五种支付方式**：现金、微信、支付宝、银行卡、储值卡余额
- 订单状态流转：待支付 → 已支付 → 已退款 / 已取消
- 退款支持全退/部分退，自动回退库存和卡次

### 4.7 财务报表

- 日报：每日销售额、支付方式分布
- 月报：月度营收汇总、趋势分析
- 月度营收趋势折线图

### 4.8 数据看板

- 营收趋势（近 30 天折线图）
- 热门课程 TOP5（柱状图）
- 会员增长趋势（折线图）
- 库存预警列表

### 4.9 入场签到

- 三种签到方式：手动输入、刷卡、QR 扫码
- QR 签到：生成令牌 → 扫码签到（无需登录）
- 防重复签到（按会员 + 日期去重）

### 4.10 系统管理

- 用户管理（CRUD、角色分配、密码重置、启用/停用）
- 角色权限管理（细粒度权限分配）
- 操作日志查询
- 系统设置（场馆名称、营业时间、到期提醒天数等）
- 通知消息（系统通知、过期提醒、预约通知、库存通知）

### 4.11 数据导入导出

- 导出：会员、订单、商品列表 → Excel
- 导入：Excel 批量导入会员

---

## 5. 数据库设计

系统共 **21 张表**，按业务域划分：

| 业务域 | 表名 | 说明 |
|--------|------|------|
| 权限体系 | `roles` | 角色 |
| | `permissions` | 权限 |
| | `role_permissions` | 角色-权限关联 |
| 用户与审计 | `users` | 系统用户 |
| | `audit_logs` | 操作日志 |
| 会员 | `members` | 会员档案 |
| | `body_measurements` | 体测记录 |
| | `check_ins` | 入场签到 |
| 会籍 | `membership_plans` | 卡种模板 |
| | `memberships` | 会员卡实例 |
| 课程 | `coaches` | 教练 |
| | `courses` | 课程 |
| | `course_schedules` | 课程排期 |
| | `bookings` | 课程预约 |
| | `course_reviews` | 课程评价 |
| 商品 | `products` | 商品 |
| | `stock_movements` | 库存流水 |
| 订单 | `orders` | 订单 |
| | `order_items` | 订单明细 |
| | `payments` | 支付流水 |
| 系统 | `settings` | 系统设置 |
| | `notifications` | 通知消息 |

---

## 6. API 接口概览

### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录 |
| POST | `/api/auth/refresh` | 刷新 Token |
| GET | `/api/auth/profile` | 个人资料 |
| POST | `/api/auth/change-password` | 修改密码 |

### 会员
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/members` | 列表 / 新建 |
| GET/PUT/DELETE | `/api/members/:id` | 详情 / 编辑 / 删除 |
| GET | `/api/members/stats` | 会员统计 |
| GET/POST | `/api/members/:id/measurements` | 体测历史 / 新增体测 |

### 会籍
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/plans` | 卡种列表 / 新建 |
| POST | `/api/memberships/issue` | 办卡 |
| POST | `/api/memberships/:id/renew` | 续费 |
| POST | `/api/memberships/:id/suspend` | 挂起 |
| POST | `/api/memberships/:id/resume` | 恢复 |
| POST | `/api/memberships/:id/transfer` | 转让 |
| POST | `/api/memberships/:id/cancel` | 作废 |

### 课程与预约
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/courses` | 课程列表 / 新建 |
| GET/POST | `/api/schedules` | 排期列表 / 新建 |
| GET/POST | `/api/coaches` | 教练列表 / 新建 |
| GET | `/api/coaches/ranking` | 教练排名 |
| POST | `/api/bookings` | 新建预约 |
| POST | `/api/bookings/:id/check-in` | 预约签到 |

### 签到
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/check-ins/qr-token` | 生成 QR 签到令牌 |
| POST | `/api/check-ins/qr/:token` | QR 扫码签到 |
| POST | `/api/check-ins` | 手动签到 |

### 商品与库存
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/products` | 商品列表 / 新建 |
| POST | `/api/products/:id/stock` | 库存变动 |

### 订单与财务
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/orders/checkout` | 混合结账 |
| POST | `/api/orders/:id/refund` | 退款 |
| GET | `/api/reports/daily` | 日报 |
| GET | `/api/reports/monthly` | 月报 |

### 数据看板
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dashboard/revenue-trend` | 营收趋势 |
| GET | `/api/dashboard/hot-courses` | 热门课程 |
| GET | `/api/dashboard/stock-alerts` | 库存预警 |
| GET | `/api/dashboard/member-growth` | 会员增长 |

### 系统管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/system/users` | 用户列表 / 新建 |
| GET/POST | `/api/system/roles` | 角色列表 / 新建 |
| GET | `/api/system/audit-logs` | 操作日志 |
| GET/PUT | `/api/system/settings` | 系统设置 |

### 其他
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload` | 文件上传 |
| GET | `/api/export/members` | 导出会员 |
| POST | `/api/import/members` | 导入会员 |
| GET | `/api/notifications` | 通知列表 |
| GET | `/api/health` | 健康检查 |

---

## 7. 快速启动

### 方式一：Docker Compose（推荐）

```bash
# 启动所有服务
docker compose up -d

# 初始化种子数据
docker compose exec server node scripts/seed.js

# 访问
# 前端：http://localhost
# API：http://localhost:4000/api/health
```

### 方式二：本地开发

```bash
# 1. 安装所有依赖
npm run install:all

# 2. 初始化数据库
mysql -uroot -p123456 < db/schema.sql

# 3. 配置环境变量
cp server/.env.example server/.env

# 4. 导入种子数据
npm run db:seed

# 5. 启动开发服务
npm run dev

# 前端：http://localhost:5173
# 后端：http://localhost:4000
```

### 运行测试

```bash
cd server && npm test
```

---

## 8. 默认账号

| 角色 | 用户名 | 密码 | 权限范围 |
|------|--------|------|----------|
| 管理员 | admin | admin123 | 全部功能 |
| 前台 | staff | staff123 | 会员、会籍、课程、商品、订单、签到 |
| 教练 | coach | coach123 | 会员查看、课程、预约 |
| 会员 | member | member123 | 课程查看、预约查看 |

---

## 9. 环境变量

```env
PORT=4000                    # 服务端口
NODE_ENV=development         # 运行环境
DB_HOST=127.0.0.1            # 数据库地址
DB_PORT=3306                 # 数据库端口
DB_NAME=jianshenfang         # 数据库名
DB_USER=root                 # 数据库用户
DB_PASSWORD=123456           # 数据库密码
JWT_SECRET=change-me-in-production  # JWT 密钥（生产环境必须修改）
JWT_EXPIRES_IN=2h            # Token 有效期
LOG_LEVEL=info               # 日志级别
```

---

## 10. 技术亮点

1. **RBAC 权限体系**：按钮级权限控制，角色权限缓存 60s
2. **JWT 双 Token**：accessToken + refreshToken，客户端请求队列防重复刷新
3. **并发安全**：课程预约扣次使用数据库行锁，防超卖
4. **自动化运维**：node-cron 定时清理过期会籍
5. **操作审计**：全链路审计日志（模块、操作、目标、IP、用户）
6. **退款自动回退**：库存和卡次自动恢复
7. **QR 无登录签到**：生成令牌，扫码即签到
8. **ECharts 数据可视化**：营收趋势、会员增长、热门课程、库存预警
9. **Excel 数据交换**：批量导入导出会员、订单、商品
10. **Docker 一键部署**：MySQL + Express + Nginx 全容器化
